# Examples module

Day-1 mesh builders are reexported by `using BEM` (`quadrado`,
`quadrado_elasticity`, `placa_com_furo`). Extra `.geo` / `.jl`
builders stay under `data/` and are used from `scripts/`.

```@docs
BEM.Examples
```
