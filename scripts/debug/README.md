# Debug / one-off probes

Scratch scripts and backup tests that are **not** part of CI. Kept so
investigations stay in git (`_chk_*`, `_dbg_*`, `tmp_*` live here after
the script-folder pass).

Do not `include` these from `test/runtests.jl`.
